VITE_BASE_URL=http://localhost:3000
VITE_CLERK_PUBLISHABLE_KEY=pk_clerk_your_publishable_key
VITE_CURRENCY=₹

# Notes:
# - Copy into `client/.env` for local development. Vite requires `VITE_` prefix for env variables exposed to the client.
# - `VITE_BASE_URL` should point to your server (default `http://localhost:3000`).
